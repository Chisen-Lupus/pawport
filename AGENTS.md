# Git workflow

- Before changing files, run `git status --short`.
- After each self-contained change, run relevant checks if practical.
- Show `git diff --stat`.
- Stage only files changed for the current task.
- Create one git commit for each completed task.
- Do not push, amend, rebase, or rewrite history unless explicitly asked.